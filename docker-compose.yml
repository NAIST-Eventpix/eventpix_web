services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
      #    networks:
      #- web
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    init: true
    ports:
      - "5001:5000"
      #networks:
      #- web
      #networks:
      #web:
      #driver: bridge
